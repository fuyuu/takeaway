<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hope.mapper.OrderItemMapper">
    <!-- 插入订单项 -->
    <insert id="insert">
        INSERT INTO order_item (order_id, spu_id, sku_id, name, image,
        price, spec, quantity)
        VALUES
        (#{orderId}, #{spuId}, #{skuId}, #{name}, #{image} ,
        #{price}, #{spec}, #{quantity} )
    </insert>
</mapper>