<configuration>
    <!-- 1. 滚动文件 Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>

        <!-- 按天 + 分片序号，单文件最大 2 GB -->
<!--        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">-->
<!--            <fileNamePattern>logs/app.%d{yyyy-MM-dd}.%i.log</fileNamePattern>-->
<!--            <maxFileSize>2GB</maxFileSize>-->
<!--            <maxHistory>30</maxHistory>   &lt;!&ndash; 时间维度：30 天 &ndash;&gt;-->
<!--            <totalSizeCap>10GB</totalSizeCap> &lt;!&ndash; 大小维度：目录总量 ≤ 10 GB &ndash;&gt;-->
<!--        </rollingPolicy>-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
          <fileNamePattern>logs/app.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
          <maxFileSize>2GB</maxFileSize>
          <maxHistory>30</maxHistory>
          <totalSizeCap>10GB</totalSizeCap>

<!--          <maxFileSize>2kb</maxFileSize>-->
<!--          <maxHistory>30</maxHistory>-->
<!--          <totalSizeCap>10kb</totalSizeCap>-->
          <cleanHistoryOnStart>true</cleanHistoryOnStart> <!-- 启动时清理过期日志 -->
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{64} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 控制台 Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
           <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %magenta([%thread]) %highlight(%-5level) %cyan(%logger{64}) - %msg%n</pattern>
<!--            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{64}) - %msg%n</pattern>-->
            <charset>UTF-8</charset>
        </encoder>
    </appender>


    <!-- 2. 异步 Appender -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>8192</queueSize>
        <neverBlock>true</neverBlock>
        <appender-ref ref="FILE"/>
    </appender>

    <!-- 3. 根日志级别和输出目标 -->
    <!--  <root level="DEBUG">-->
    <root level="INFO">
        <appender-ref ref="ASYNC_FILE"/><!-- 输出到文件 -->
        <appender-ref ref="CONSOLE"/><!-- 输出到控制台 -->
    </root>
</configuration>